# Python FastAPI backend to a pokedex system

Simple API, with a CRUD using FastAPI, Swagger and MongoDB

Dockerized system, to use simply use the commands:

- docker-compose build

- docker-compose up